name: Golden validation

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: PHP ${{ matrix.php }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php:
          - '8.4'
          - '8.5'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none
          tools: wp-cli

      - name: Install gettext
        run: sudo apt-get update && sudo apt-get install -y gettext

      - name: PHP syntax
        run: find . -type f -name '*.php' -not -path './dist/*' -not -path './build/*' -print0 | xargs -0 -n1 php -l

      - name: Golden Marketplace regression
        run: php tests/golden-marketplace-regression.php

      - name: Product regressions
        run: |
          for test in tests/*-regression.php; do
            php "$test"
          done

      - name: Release build
        run: bash tools/build-release

  runtime:
    name: Runtime PHP ${{ matrix.php }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php:
          - '8.4'
          - '8.5'

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: wordpress
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none
          extensions: mysqli, sodium
          tools: wp-cli

      - name: Install disposable WordPress
        run: |
          wp core download --path=/tmp/wordpress --force
          wp config create             --path=/tmp/wordpress             --dbname=wordpress             --dbuser=root             --dbpass=root             --dbhost=127.0.0.1:3306
          wp core install             --path=/tmp/wordpress             --url=http://seo-runtime.test             --title="SEO Runtime"             --admin_user=admin             --admin_password='runtime-test-password'             --admin_email=runtime@example.test             --skip-email

      - name: Install Base, conflict fixture and SEO
        run: |
          git clone --depth 1 https://github.com/Core-Blueprint/wp-core-blueprint.git /tmp/wordpress/wp-content/plugins/core-blueprint
          wp plugin activate core-blueprint --path=/tmp/wordpress
          wp cb operator recover 1 --path=/tmp/wordpress

          mkdir -p /tmp/wordpress/wp-content/plugins/wordpress-seo
          cat > /tmp/wordpress/wp-content/plugins/wordpress-seo/wp-seo.php <<'PHP'
          <?php
          /**
           * Plugin Name: Golden SEO Conflict Fixture
           * Version: 1.0.0
           */
          defined( 'ABSPATH' ) || exit;
          PHP
          wp plugin activate wordpress-seo --path=/tmp/wordpress

          mkdir -p /tmp/wordpress/wp-content/plugins/core-blueprint-seo
          rsync -a             --exclude='.git/'             --exclude='dist/'             --exclude='build/'             "$GITHUB_WORKSPACE/"             /tmp/wordpress/wp-content/plugins/core-blueprint-seo/
          wp plugin activate core-blueprint-seo --path=/tmp/wordpress

      - name: Verify conflict fail-safe
        run: wp eval-file "$GITHUB_WORKSPACE/tests/runtime-conflict-wordpress.php" --path=/tmp/wordpress --user=1

      - name: Remove conflicting SEO provider
        run: wp plugin deactivate wordpress-seo --path=/tmp/wordpress

      - name: Verify conflict-free runtime and JSON-LD safety
        run: wp eval-file "$GITHUB_WORKSPACE/tests/runtime-seo-wordpress.php" --path=/tmp/wordpress --user=1
